# Simple To-Do API

A lightweight RESTful API built with Python and Flask for managing a to-do list. This project uses in-memory storage, making it perfect for quick demonstrations and learning the basics of CRUD (Create, Read, Update, Delete) operations.

## 🚀 Features
* **List all tasks**: View the entire to-do list.
* **Find a task**: Look up a specific task by its unique ID.
* **Add tasks**: Create new items dynamically.
* **Update tasks**: Modify the title or completion status of existing items.
* **Delete tasks**: Remove items from the list.

## 🛠️ Requirements
* Python 3.x
* Flask

## ⚙️ Installation & Setup

1. **Clone the repository**:
   ```bash
   git clone <https://github.com/fredrick-rangara/intro-to-flask>
   cd <intro-to-flask>